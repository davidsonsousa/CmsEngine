@if (!string.IsNullOrWhiteSpace(TempData[MessageConstants.InfoMessage].ToString()))
{
    <div class="alert alert-info alert-dismissible fade show" role="alert">
        @TempData[MessageConstants.InfoMessage]
        <button type="button" class="close" data-dismiss="alert" aria-label="Close">
            <span aria-hidden="true">&times;</span>
        </button>
    </div>
}

@if (!string.IsNullOrWhiteSpace(TempData[MessageConstants.SuccessMessage].ToString()))
{
    <div class="alert alert-success alert-dismissible fade show" role="alert">
        @TempData[MessageConstants.SuccessMessage]
        <button type="button" class="close" data-dismiss="alert" aria-label="Close">
            <span aria-hidden="true">&times;</span>
        </button>
    </div>
}

@if (!string.IsNullOrWhiteSpace(TempData[MessageConstants.WarningMessage].ToString()))
{
    <div class="alert alert-warning alert-dismissible fade show" role="alert">
        <strong>Warning:</strong> @TempData[MessageConstants.WarningMessage]
        <button type="button" class="close" data-dismiss="alert" aria-label="Close">
            <span aria-hidden="true">&times;</span>
        </button>
    </div>
}

@if (!string.IsNullOrWhiteSpace(TempData[MessageConstants.DangerMessage].ToString()))
{
    <div class="alert alert-danger alert-dismissible fade show" role="alert">
        <strong>Error:</strong> @TempData[MessageConstants.DangerMessage]
        <button type="button" class="close" data-dismiss="alert" aria-label="Close">
            <span aria-hidden="true">&times;</span>
        </button>
    </div>
}
